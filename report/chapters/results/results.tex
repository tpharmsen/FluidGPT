\chapter{Results}

- join meeting wednesday and resubmit form 4 with new dates
 - redefine research questions
 - describe models
 - regenerate figures
 - write story about results figure 
 	- ss
 	- ms
 	- physics
 - describe datasets in detail

for each following point compare the AR vs FM:
- show per-dataset single next timeblock error
- show per-dataset rollout error with t on x-axis
- show energy and enstrophy spectra graph for single trajectory
-> then followup with log error plot for spectra

REGENERATE FIGURES FIRST -> define story -> REGEN FIG FINAL

\section{Single-step prediction}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{figs/results/ssbox_rae.png}
	\caption{Boxplot of relative absolute error of the single next-timeblock prediction for each of the four surrogate foundation models, grouped by dataset. The models are trained on the same trajectory-datasplit and evaluated in zero-shot fashion on the validation datset. For the flowmatching models, FM-d2 and FM-d3, 16 samples are taken for each input-output pair. The graph mostly the flowmatching model FM-d2 outperforming the other models, except on the pdebench-incomp and pdegym-sines dataset. All models seem to have difficulty predicting the more complex pdebench-comp dataset.}
	\label{fig:ssbox}
\end{figure}


- explain the probabilistic models perform better than deterministic models on almost all datasets
- higher dimensional datasets with more turbulence or compressibility instead of incompressibility condition have higher error. 
- probablistic model does not perform better on pdegym sines dataset, possibly due to wrong frequency initalization of prior distribution flowmatching
- also we can see that increasing depth and amount of parameters on probabilistic models yield a greater improvement in score compared to increasing depth and amount of parameter for deterministic models.

Figure \ref{fig:ssbox} reports the per-sample relative per-sample error across all nine datasets, 
revealing several consistent trends in model performance.

\textbf{Probabilistic vs.\ deterministic models.} The flowmatching model FM-d2 outperforms the deterministic counterparts on nearly all datasets. This is expected given the 
inherent stochasticity of turbulent fluid dynamics: a deterministic model is constrained 
to produce a single point estimate of the subsequent system state, whereas a probabilistic 
model learns a distribution over plausible futures and can better capture the multi-modal 
nature of the solution space. By conditioning on a learned prior and flowing towards the 
target distribution, the flow matching models are better equipped to represent uncertainty 
in the predicted state block. %The larger error bars of the probabilitic models on the compressible dataset (pdebench-comp) indicate a larger uncertainty within the diverging state-space of the navier-stokes time progression. 

\textbf{Effect of dataset complexity.} Error magnitudes are notably higher on 
higher-dimensional datasets characterised by strong turbulence or compressible flow 
conditions, compared to datasets satisfying the incompressibility assumption. This aligns 
with the increased complexity of the underlying dynamics: compressible and highly turbulent 
regimes exhibit sharper gradients and finer-scale structure that are more challenging to 
predict across multiple time steps simultaneously.

\textbf{Exception: PDEGym sines dataset.} The probabilistic models do not demonstrate 
a clear advantage on the PDEGym sines dataset. We attribute this to a mismatch between 
the dominant frequencies present in the data and the frequency initialisation of the prior 
distribution used in the flow matching procedure. Due to a bigger mismatch in frequencies and the inability of the model to create shard interfaces in the fluid, the transport problem becomes a more difficult task, yielding a less inaccurate output of the model.

\textbf{Scaling behaviour.} A further distinction between the two model families emerges 
when comparing shallow and deep variants within each. Changing model depth and parameter 
count yields a more pronounced reduction in error for the probabilistic models than for 
the deterministic ones. This suggests that the additional capacity is more effectively 
utilised when the model has the expressive power to refine a distributional estimate, 
whereas deterministic models may saturate earlier or be limited by the intrinsic 
irreducibility of the static point-estimation objective.

\section{Rollout prediction}

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{figs/results/msgraph_all_rae.png}
	\caption{3x3 subplot of the per-sample relative absolute error of rollout predictions for all models grouped per dataset. The models are used in autogressive fashion to predict the full-length trajectory, starting with the first available input timeblock containing the initialization conditions. The flowmatching models are taking 16 global rollout samples for each existing trajectory in the zero-shot validation dataset and use a new prior distribution sample for each individual input-output prediction during the autoregressive rollout. Flowmatching model FM-d2 outperforms all other models, except for pdegym-sines. Error accumulation patterns are different across datasets and models. }
	\label{fig:rollout}
\end{figure}

- In most datasets the error accumulates less quickly for the flowmatching models than the deterministic ones
- some datasets have less diverging system states according to the probalistic models
- It seems the deterministic models are very similar to each other, while the probabilistic models differ significantly to eachother. An increase in parameters leads to significant reduce in error accumulation for the probablistic models
- Once again the pdegym sines dataset works better to predict deterministic, and the pdebench-incomp dataset has a very high accumulation of error (due to the fact that this dataset includes forcing of fluid and the model is not informed of this forcing)
- on the shorter temporal length datasets we can see that we have spikes on the outsides of the temporal block prediction, indicating strong information spread edge conditions, especially for the deterministic models.

Figure \ref{fig:rollout} presents the autoregressive rollout MSE across all nine 
datasets, where each model is applied recurrently to predict successive blocks of system 
states. The rollout length varies between datasets, reflecting differences in the 
temporal extent of each benchmark.

\textbf{Error accumulation rate.} The flow matching models accumulate error more slowly 
over the course of the rollout on the majority of datasets. Beyond the lower absolute 
error observed in the previous section, this suggests that the probabilistic models 
produce predictions that are more dynamically consistent, limiting the compounding of 
errors across autoregressive steps. On several datasets, the probabilistic models also 
yield less divergent rollout trajectories, indicating that the predicted system states 
remain closer to a physically plausible regime for longer.

\textbf{Intra-family scaling behaviour.} A clear asymmetry emerges in how the two model 
families respond to increased capacity. The two deterministic models produce nearly 
identical rollout curves across datasets, suggesting that additional depth and parameters 
provide limited benefit within the deterministic framework and that these models may be 
approaching the ceiling of what point-estimation objectives can recover. In contrast, the 
two probabilistic variants differ substantially from one another: the deeper, 
higher-parameter flow matching model exhibits a marked reduction in error accumulation 
relative to its smaller counterpart, confirming the scaling trend observed in the SS 
error analysis and indicating that additional capacity is more productively absorbed when 
learning a distributional representation of the dynamics.

\textbf{Boundary artefacts in short-horizon datasets.} On datasets with shorter temporal 
block lengths, the rollout curves exhibit characteristic spikes at the edges of each 
predicted block. These artefacts are consistent with strong information spread at the 
temporal boundaries of the block prediction: the model has limited context near the edges 
of each window, producing elevated local error that then partially recovers toward the 
block interior. This effect is most pronounced for the deterministic models, suggesting 
that the probabilistic formulation provides some degree of regularisation at block 
boundaries, possibly through the smoothing effect of the learned prior.

\textbf{Dataset-specific observations.} Two datasets warrant individual discussion. First, 
the PDEGym sines dataset again favours the deterministic models, consistent with the 
hypothesis raised in Section that the prior frequency initialisation is 
misspecified for this benchmark. Second, the PDEBench incompressible dataset exhibits 
the highest error accumulation of all evaluated benchmarks across all models. This is 
attributable to the presence of external body forcing in this dataset: the fluid is 
subject to a forcing term that drives the dynamics, yet none of the models are provided 
with this forcing signal as input. The models must therefore account for the effect of 
an unobserved driver, making accurate rollout prediction substantially harder and 
introducing a persistent source of error that compounds with each autoregressive step.

\section{Spectrum analysis}\label{se:spectraresults}

Figure \ref{fig:energyspectrum} presents the energy spectrum error ratio for three special case datasets: \textit{amira}, \textit{pdegym-sines}, and \textit{pdegym-bb}. The energy spectra are computed from both the predicted and reference trajectories at selected rollout timesteps, after which the ratio between the two spectra is calculated for each wavenumber. Values above one indicate an overprediction of energy at a particular wavenumber, while values below one indicate an underprediction. The three respective datasets have been chosen for spectrum-inspection due to the difference in error accumulation during rollout

\begin{figure}[H]
	\centering
	\includegraphics[width=0.8\textwidth]{figs/results/spectra/spectrum_E.png}
	\caption{Log-log plot of the energy spectrum error ratio per wavenumber for three special case datasets for models FM-d2 and AR-d2 only. Using the incompressibility assumption, the energy spectrum is calculated at two specific timesteps in the rollout prediction and the reference trajectory, of which the the error ratio is calculated. For the dataset amira at t=20 and t=200 the energy spectrum values are calculated, and for the rest of the datasets at t=5 and t=20. For the flowmatching model 16 rollout samples are drawn, from which the median is plotted in the graphs. Values above 1 indicates overprediction of energy at that wavenumber and under 1 means underprediction compared to the true energy spectrum. A trend emerges that higher wavenumbers are increasingly overpredicted, indicating that higher frequency might not be predicted as well as the lower frequencies.}
	\label{fig:energyspectrum}
\end{figure}


!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!!! new legend for other datasets t=5 missing
In Figure \ref{fig:energyspectrum} three special cases are shown for datasets amira, pdegym-sines and pdegym-bb
- Explain large and middle size eddies are perfectly predicted in the diagram, but there is difficulty in prediction in the dissipation zones
- explain that the energy at higher wavenumbers is overpredicted for all datasets and results in accumulation of error in rollout use.
- for pdegym-sines there is also a slight underprediction also at higher wavenumbers
- explain that the reason that the flowmatching models are better is because at the first deviation from left to right on the error ratio figures the deterministic models are more off than the flowmatching models. This is especially visible in the amira dataset. The overall rollout error as seen in Figure \ref{fig:rollout} is smaller for the rest of the datasets, and thus the difference in these graphs are less visible.
- explain that a single small pixel error in prediction induces a wide range of frequencies in the frequency spectrum. 
- Also explain that the higher wavenumbers have way less energy in a typical system, so the error ratio being very high in the figure doesnt mean a big error in the non-frequency domain of the predictions
- explain also that in the higher wavenumbers (dissipation zone) the energy spectrum converges to 0, causing the ratio to explode because of dividing by nearly 0




For all three datasets, the energy-containing and inertial-range scales are predicted accurately. The error ratio remains close to unity for low and intermediate wavenumbers, demonstrating that the models successfully capture the large and medium-sized flow structures that contain most of the system's energy. The deviations occur in the last bit of intertial-range to dissipation range at higher wavenumbers, where the models exhibit increasing difficulty in accurately reproducing the smallest scales of the flow.

A consistent trend across all datasets is the overprediction of energy at higher wavenumbers. This indicates that the models introduce excess high-frequency content during rollout, causing errors to accumulate over time. Such behaviour is expected to contribute to the gradual degradation of rollout quality observed in Figure \ref{fig:rollout}. The \textit{pdegym-sines} dataset additionally exhibits a slight underprediction at certain middle to high wavenumbers before transitioning to overprediction.

The overall superiority of the Flow Matching models becomes apparent when examining the first significant deviation of the error ratio from unity. At this point, the deterministic autoregressive models generally deviate more strongly than their Flow Matching counterparts, indicating a more accurate preservation of the spectral energy distribution. This effect is particularly pronounced for the \textit{amira} dataset, where the difference between the models is clearly visible. For the dataset \textit{pdegym-bb}, the overall rollout errors shown in Figure \ref{fig:rollout} are smaller, which consequently reduces the visual separation between the spectral error curves. For the dataset \textit{pdegym-sines} the deterministic model performs better, which can be derived from the more accurate middle wavenumbers compared to the flowmatching model. This might be linked to the inability of the flowmatching model architecture to handle certain frequencies that are dominant in the pdegym-sines dataset.

It should be noted that spectral errors can appear amplified compared to errors in the spatial domain. A small local prediction error, such as a single-pixel deviation, contributes energy across a broad range of frequencies after the Fourier transform is applied. Consequently, relatively minor spatial inaccuracies can manifest as widespread deviations in the energy spectrum. Furthermore, higher wavenumbers typically contain substantially less energy than lower wavenumbers in turbulent and dissipative systems as seen in Figure \ref{fig:energyspectrumscheme}. Therefore, a large error ratio at high wavenumbers does not imply a large error in the original spatial field.

Finally, caution should be exercised when interpreting the largest error ratios in the dissipation range. As the true energy spectrum approaches zero at sufficiently high wavenumbers, the denominator of the error ratio becomes very small. Even minor absolute differences between the predicted and reference spectra can therefore produce extremely large ratio values. The apparent divergence of the curves at the highest wavenumbers should thus be interpreted as a consequence of dividing by near-zero energy levels rather than as evidence of catastrophic prediction errors.








